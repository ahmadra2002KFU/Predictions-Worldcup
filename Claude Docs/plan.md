# خطة البناء والحالة — مفيد لتوقعات كأس العالم

الخطة الكاملة المعتمدة موجودة في: `C:\Users\Ahmad\.claude\plans\shiny-baking-harbor.md`
(صُمّمت بمساعدة وكيل تخطيط + مراجعة مهندس أول عبر Codex + جولتي أسئلة مع المستخدم).

## حالة المراحل — كلّها مكتملة ومُتحقَّق منها في المتصفح عبر Claude in Chrome

| # | المرحلة | التحقّق |
|---|---------|---------|
| 0 | تهيئة المشروع + RTL/العلامة + Docker أساسي | يعمل عبر الحاوية ويظهر بالعربية |
| 1 | نموذج البيانات (Prisma + ترحيل + بذور) | الجداول موجودة والبذور تعمل |
| 2 | الهوية (تسجيل/استرجاع/جلسة) | تسجيل → تحديث → ما زال معروفاً؛ السجل يحفظ IP |
| 3 | مصادقة الإدارة + CRUD للفرق والمباريات | إضافة فريق، لصق قائمة لاعبين، إنشاء مباراة |
| 4 | التوقّعات + قفل 90 ثانية | حفظ التوقّع؛ رفض 409 بعد القفل (متحقَّق بالخادم) |
| 5 | محرّك النقاط + اختبارات | 21 اختبار ناجح؛ نتيجة عادية 3ن؛ ركلات ترجيح 4ن |
| 6 | لوحة الصدارة | مباراة منتهية بلا توقّعات = صفر للجميع؛ التعادل في الترتيب صحيح |
| 7 | الدردشة + التحديث اللحظي (SSE) | رسالة تظهر في تبويب آخر؛ الصدارة تتحدّث لحظياً عند إدخال نتيجة |
| 8 | بطاقات المباريات + أعلام + حركات | أعلام حقيقية (flag-icons)، دخول متدرّج، إعادة ترتيب الصدارة |
| 9 | لوحة الإدارة (سجل النشاط + المشاركون) | عناوين IP المشبوهة تظهر؛ إعادة تسمية تعمل |
| 10 | التحصين (حدود المعدل، التحقّق، RTL) | الحد من المعدل يعطي 429؛ lint/types/tests خضراء |
| 11 | تحقّق شامل عبر المتصفح | كل المسارات مُختبرة بما فيها ركلات الترجيح |
| 12 | Docker كامل + دليل النشر | المكدّس يُبنى ويعمل (db+migrate+app)؛ Caddy جاهز |

## ملاحظات
- التطبيق مُصمَّم لنسخة واحدة من حاوية `app` (SSE + حدّ المعدل في الذاكرة).
- قاعدة البيانات في الإنتاج تبدأ فارغة؛ البيانات تُدخل من لوحة الإدارة.
- المتبقّي على المستخدم: توفير VPS + دومين + تشغيل خطوات `Claude Docs/deployment.md`.
